A lightweight starter template for React DOM projects using snowpack as an alternative to the bloated `create-react-app`.

#### Dependecies

- `react`
- `react-dom`

#### Dev Dependencies

- `snowpack`

#### Usage

```bash
yarn install
```
